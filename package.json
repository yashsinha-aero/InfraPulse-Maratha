{
  "name": "infrapulse",
  "version": "1.0.0",
  "private": true,
  "description": "Photo-Based Defect Detection & Priority Maintenance Web System",
  "scripts": {
    "dev": "node start.js",
    "start": "node start.js",
    "backend": "cd backend && .venv/bin/uvicorn app.main:app --port 8005 --reload",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
